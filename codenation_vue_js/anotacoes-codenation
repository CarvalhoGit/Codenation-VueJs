bulma css:
	level
	hero
	tiles

inVision Studio: prototipo para estrutura visual do site

vsconde extensoes: auto close tag, auto rename tag, polacode, html snippets, ident rainbow, sync settings

package.json - scripts: define um script para ser executado ("dev": "live-server")

document.get
document.query
